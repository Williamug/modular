<?php

use Williamug\Modular\HookManager;
use Illuminate\Support\Facades\Log;

return function (HookManager $hooks) {
  $hooks->listen('user.created', function ($user) {
    Log::info("New user registered: {$user->name}");
  });
};
